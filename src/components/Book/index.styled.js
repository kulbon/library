import styled from 'styled-components';
import Badge from 'react-bootstrap/esm/Badge';

export const StyledGenres = styled('div')``;

export const StyledGenre = styled(Badge)`
  margin: 0 2px 2px 2px;
`;
